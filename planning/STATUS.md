# Planning Status

Master index of tracked planning work in PHX. Update when a unit of work is added, changes status, or is folded back into `context/`/`docs/`.

_Last updated: 2026-09-10_

## Active / current work

| Item | Kind | Status | Issue | Pointer |
|------|------|--------|-------|---------|
| PHPP Components writer leaves stale rows on shrinking export | Bug fix | **Merged to main** (2026-08-27, PR [#100](https://github.com/PH-Tools/PHX/pull/100), closes [#99](https://github.com/PH-Tools/PHX/issues/99)) — the three Components list-writers block-read the section tail after writing, warn via `xl.output`, and take keyword-only `clear_stale=False` that blanks only the row models' own input columns (formula columns never touched). 1086 tests + xl-replay golden state green. Release via semantic-release on the merge. Field origin: honeybee_grasshopper_ph#59 (packet archived there) | [#99](https://github.com/PH-Tools/PHX/issues/99) (closed) | `PHX/PHPP/sheet_io/io_components.py`, `tests/test_PHPP/test_sheet_io/test_io_components_stale_rows.py` |
| `from_PHPP` results reader (Pholio spike 2) | Feature (new source family) | **Implemented on branch `feat/from-phpp-results-reader`, PR pending** — closed-file reader: sniff (version/language/project vs blank template vs not-a-PHPP), headline `Verification`+`PER`+`Cooling load` results with source cell + caption asserted, stale-cache verdict, typed refusals; `xl/xl_readable.XLReadable` read Protocol; 46 tests (synthetic PHPP-shaped public fixtures + gitignored private corpus). Results map verified on 10.6 EN only; 10.x EN assumed, 9.x/IP refused by name | [#101](https://github.com/PH-Tools/PHX/issues/101) | `PHX/from_PHPP/`, `tests/test_from_PHPP/` |
| Public live Honeybee → PHX API | API/docs cleanup | **Complete** — verified and archived | — | [`archive/honeybee-conversion-facade/`](archive/honeybee-conversion-facade/PRD.md) |
| Aperture-bearing xl-replay golden fixture | Follow-up (manual) | **Open** — needs live Excel + licensed PHPP (`scripts/perf/record_replay_fixture.py`); covered by unit tests meanwhile | [#102](https://github.com/PH-Tools/PHX/issues/102) | [`archive/aperture-psi-install/`](archive/aperture-psi-install/aperture-psi-install-plan.md) |
| `Addl vent` room rows overflow the shipped 30-row block | Bug fix | **Scoped** (writeup only, 2026-09-02) — a 44-space export writes rooms at rows 31:74 on a blank 10.6 template while the shipped aggregations only count 31:60, so rooms 31+ silently vanish from unit totals; the overflow also clobbers the instruction row and unit-header labels (incl. merged cells). Direction: warn via `xl.output` when a section write would overflow the allowed area (PR #100 pattern); capacity is discoverable from the existing section locators. Found via the phx-phpp-bridge Phase 0a replay (Gate G-A record) | [#115](https://github.com/PH-Tools/PHX/issues/115) | [`bug-fix/addl-vent-room-row-overflow/`](bug-fix/addl-vent-room-row-overflow/README.md) |
| PHPP writer input gaps | Bug fix (6 items) | **Scoped** — the incoming request verified against the code and the blank 10.6 template; three of its five diagnoses changed. `Verification` is a version-guard defect dropping 12 inputs, not a missing `F29` writer; `Ventilation` is two dead cells (`J19` *and* `J20`), not one; `Climate` additionally corrupts `P67`. `SummVent` and `Ground` confirmed as filed. **`06` added 2026-08-15:** `Verification!N30` mechanical cooling — never written and no model field; drives `Ground!N122` (ground-iteration interior-temperature clamp), which is how OpenPH found it. Needs a honeybee-ph `SetPoints.mechanical_cooling` field first. `05` (`Ground` writer) now depends on the foundation-shape feature below | [#103](https://github.com/PH-Tools/PHX/issues/103) | [`bug-fix/phpp-writer-input-gaps/`](bug-fix/phpp-writer-input-gaps/README.md) |
| Foundation model shape for PHPP 10.x `Ground` | Feature (cross-repo, honeybee-ph **primary**) | **Scoped** — the honeybee-ph/PHX foundation model is WUFI-shaped; PHPP 10.6 `Ground` asks for seven inputs it lacks ("interior wall towards heated" A/U on slab / unheated basement / crawl space, crawl-space wind shield factor). Field-by-field gap table, required fields with names/defaults, three small model defects, four open decisions for Ed. Blocks `05` Phase 2 and OpenPH's foundation objects | [#104](https://github.com/PH-Tools/PHX/issues/104) | [`features/foundation-phpp10-shape/`](features/foundation-phpp10-shape/README.md) |
| Five PHPP inputs the writer never writes | Feature (request) | **Superseded** — scoped into the bug-fix packet above | — | [`features/phpp-writer-input-gaps/`](features/phpp-writer-input-gaps/README.md) |
| WUFI import: Space-list reconciliation | Bug fix (3 items) | **Items 1–2 implemented; item 3 open** — scope decided: `from_WUFI_XML` is a general-purpose importer, not a round-trip path. Fixed: Spaces with no person-load record held an unregistered Occupancy Pattern, so WUFI/METr wrote a dangling `IdentNrUtilizationPattern` (broken since PR #77, surfaced by the new export validation); and the `Name`-matching heuristic beneath it (double-consumed load records, exact-string joins, occupancy zeroed by a blank `AreaRoom`, silent non-reconciliation). 977 tests green. Item 3 is the structural follow-up: `PhxSpace` conflates a ventilation room with a utilization zone, so the pairing stays a guess for any file PHX did not write | [#105](https://github.com/PH-Tools/PHX/issues/105) | [`bug-fix/wufi-import-space-reconciliation/`](bug-fix/wufi-import-space-reconciliation/README.md) |
| WUFI export fails identity validation (≥200 kW cooling) | Bug fix (regression) | **Phases 0-2 implemented + verified on branch `bug-fix/export-time-identity-scope`** — regression from PR #86 (v1.56.82): `_bug_fixes.split_cooling_into_multiple_systems` built model objects after the conversion scope closed, so the legacy class counters (now `0`) re-issued IDs the project already used. Fixed: `PhxProject.identity_scope()` no longer lazily attaches an empty allocator (no-ops for hand-assembled projects), and the split now allocates inside the project scope. Also fixed the capacity/airflow divisor it un-masked (was inflating installed cooling 1.5-2x with ≥2 cooling devices). 1052 tests green; Arverne D exports, 1628.5 kW conserved. **Phase 3 (900% zone cooling coverage) blocked** on whether WUFI honours system `ZoneCoverage` or device `cooling_percent` | [#106](https://github.com/PH-Tools/PHX/issues/106) | [`bug-fix/export-time-identity-scope/`](bug-fix/export-time-identity-scope/README.md) |
| `PhxSpace.__add__` mutates the source model during serialization | Bug fix | **Implemented** — the ERV space-merge runs while writing the file and hands the merged space a *reference* to the first source space's ventilation program, then writes the summed load through it. Exporting one `PhxProject` twice compounds the flows (53.24 → 106.48 → 159.72); WUFI-then-PHPP writes the inflated value into `Addl vent` (PHPP reads the unmerged source spaces). Latent since v1.45.0 (2024-06-05). Shipped CLI/Grasshopper paths are one-export-per-process and unaffected. Fixed: `__add__` builds its own `PhxProgramVentilation` with the summed load and a shared *schedule* (a `deepcopy` would re-introduce the PR #90 dangling-pattern bug), and both exporters' single-space branch renames a copy. Export bytes unchanged; 10 new tests. **Follow-up open:** move the ERV merge out of serialization into conversion | [#107](https://github.com/PH-Tools/PHX/issues/107) | [`bug-fix/space-merge-mutates-source-graph/`](bug-fix/space-merge-mutates-source-graph/README.md) |
| Align `FloorAreaUtilizationZone` between WUFI and METr | Bug fix | **Filed** — exporter contract needs a source-of-truth decision | [#108](https://github.com/PH-Tools/PHX/issues/108) | issue-only (full evidence in [#108](https://github.com/PH-Tools/PHX/issues/108)) |
| Component-ID lookups can still build `None-<name>` | Bug fix | **To investigate** — same silent exposure remains in the glazing / frame / constructor lookups; three per-lookup writeups filed, constructor is highest-priority (its search column also holds material-layer names). Needs a scope decision (bound only, or also hard-fail) | [#109](https://github.com/PH-Tools/PHX/issues/109) | [`bug-fix/component-id-lookup-hardening.md`](bug-fix/component-id-lookup-hardening.md) |
| Optional `honeybee-ph` dependencies (Pollination) | Feature (API surface) | **Scoped** — drafted 2026-04-02, still unimplemented (re-verified 2026-08-25: no `HAS_PH` in `PHX/`). Lets Pollination users build a "bare" PHX model — geometry + assemblies only — without `honeybee-ph` installed. 29 module-level imports across 10 `from_HBJSON/` files, ~54 runtime `.properties.ph` accesses; guard at `create_project.py` + `create_variant.from_hb_room()`. Awaiting a decision to proceed | [#110](https://github.com/PH-Tools/PHX/issues/110) | issue-only (full scope in [#110](https://github.com/PH-Tools/PHX/issues/110)) |
| METr slab U-value `sel=2` ("Detect Automatically") | Bug fix (open question) | **Blocked — external.** The METr writer emits `sel=2` for `slabFlU`/`bmentFlU` when the model has no user U-value, mirroring WUFI; the option does not appear in the METr UI. Asked the dev team 2026-03-30, unanswered as of 2026-08-25. The sibling `lrtb*` order question is resolved (GUI order L/R/T/B, verified 2026-08-03) | [#111](https://github.com/PH-Tools/PHX/issues/111) | issue-only (full record in [#111](https://github.com/PH-Tools/PHX/issues/111)) |
| Declared-U Assembly: honor the declared Layer thickness, collapse the sandwich shells | Feature (additive) | **Implemented on branch `feat/declared-u-layer-thickness`** (2026-09-10) — PH-Navigator for SketchUp (decision E4-2) emits a Declared-U Assembly as the `CreateSDConstructions` sandwich, with the real thickness on the no-mass material's `properties.ph.user_data["phn_declared_u"]`. PHX gave every no-mass Layer the 0.1 m default, so a 305 mm assembly reported 120 mm across three rows. Fixed in `from_HBJSON/create_assemblies.py`: the marker is read for thickness only (R always comes from the Honeybee material, so the U-value never moves) and an exact marked 3-material sandwich collapses to one Layer before the layers and the exterior radiation properties are built. Every malformed-marker path falls back to the 0.1 m default with a warning. 17 new tests, 1111 green; reference XML/METr output and the xl-replay golden state unchanged, since no reference model carries the marker. Unmarked `MAT_Mass` sandwiches deliberately untouched. **Adjacent finding still needing its own issue:** PHX writes `Rsi = Rse = 0` to PHPP, so a film-stripped declared U lands +0.0039 W/m²K (+2.6%) in the workbook while WUFI reproduces it exactly | [#116](https://github.com/PH-Tools/PHX/issues/116) | [`features/declared-u-layer-thickness/`](features/declared-u-layer-thickness/README.md) |
| Decouple "Dwelling" from `Room.zone` | Refactor (cross-repo) | **Implemented** — awaiting step 3 (`honeybee_grasshopper_ph`), then the `hbph_test_models.gh` end-to-end run | [#112](https://github.com/PH-Tools/PHX/issues/112) | [`refactor/dwelling-zone-decoupling.md`](refactor/dwelling-zone-decoupling.md) |

## Completed / archived work

| Item | Kind | Status | Pointer |
|------|------|--------|---------|
| PHPP ventilator ID resolves to `None-<name>` | Bug fix | **Complete** — entry-section bounding + loud failure, plus five locator off-by-ones; the originally-filed trigger never reproduced. 971 tests green, replay invariant unchanged; archived | [`archive/phpp-ventilator-id-lookup/`](archive/phpp-ventilator-id-lookup/README.md) |
| Project-scoped deterministic identities | Architecture feature | **Complete** — isolated HB/WUFI allocation, explicit claims, exporter validation, and full regression suite; archived | [`archive/project-scoped-identities/`](archive/project-scoped-identities/README.md) |
| Explicit ventilation assignment semantics | Feature (cross-repo) | **Complete** — PHX v1.56.79 released with `honeybee-ph>=1.33.42`; published OpenPH v0.5.1 matrix verified | [`archive/ventilation-assignment-semantics/`](archive/ventilation-assignment-semantics/README.md) |
| HBJSON Space loads + utilization schedules | Bug fix | **Complete** — seven phases implemented and verified; packet archived | [`archive/hbjson-occupancy-and-schedules/`](archive/hbjson-occupancy-and-schedules/README.md) |
| Aperture-level Psi-Install (Install Types) | Refactor (cross-repo) | **Complete** — merged (PR #80) + released v1.56.73; archived | [`archive/aperture-psi-install/`](archive/aperture-psi-install/aperture-psi-install-plan.md) |
| Psi-install bug fixes | Refactor (cross-repo) | **Complete** — eight phases published across PHX, honeybee_ph, and honeybee_grasshopper_ph | [`archive/psi-install-bug-fixes/`](archive/psi-install-bug-fixes/psi-install-bug-fixes-plan.md) |
| Write ventilation ducting to PHPP "Addl vent" | Feature | **Complete** — PHPP 10.6 row/formula check and full suite pass | [`archive/phpp-vent-ducting/`](archive/phpp-vent-ducting/README.md) |

## Cross-repo work

`aperture-psi-install` spans four repos. PHX holds the export-side heavy lifting: resolved
per-edge Ψ-install on the aperture element, PHPP per-row write, and the WUFI/METr window-type
variant synthesis. Blocked on the `honeybee_ph` primary shipping.

| Repo | Doc | Role |
|------|-----|------|
| `honeybee_ph` | `planning/refactor/aperture-psi-install.md` | Primary — data model + resolver + tests |
| `PHX` | [`archive/aperture-psi-install/`](archive/aperture-psi-install/aperture-psi-install-plan.md) | PHPP per-row write; WUFI/METr variant synthesis — **complete, archived** |
| `honeybee_grasshopper_ph` | `planning/refactor/aperture-psi-install.md` | Components; deletes the bug-#59 mechanism |
| `ph-navigator-v2` | `planning/features_v1.1/aperture-psi-install/upstream-alignment.md` | Phase-07 GH-client mapping |

`dwelling-zone-decoupling` spans three repos. PHX is the **downstream consumer**: it never
reads `Room.zone` (verified, 0 hits), so its role is to prove the upstream change is safe and
to retire a duplicated dwelling-aggregation helper. Deferrable without blocking the others.

| Repo | Doc | Role |
|------|-----|------|
| `honeybee_ph` | `planning/refactor/dwelling-zone-decoupling.md` | Primary — shared helper + tests |
| `honeybee_grasshopper_ph` | `planning/dwelling-zone-decoupling.md` | Root cause — the two `Room.zone` references |
| `PHX` | [`refactor/dwelling-zone-decoupling.md`](refactor/dwelling-zone-decoupling.md) | Downstream consumer — clearance + dedup |

## Note on legacy dated notes

Historical working notes live in the gitignored `plans/<YYYYMMDD>/` folders (e.g. pydantic-v2 migration, METr-JSON exporter, excel-interop refactor). Those are scratch/reference, not tracked planning — new tracked work should use `features/`/`refactor/` here.

## Update rule

When an item reaches `Complete`, fold its outcome into the relevant `context/` doc (or authoritative `docs/` deep-dive), then move its folder to `archive/<slug>/` and add a row to `archive/README.md`.
